<componente:_ComponenteBase
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:componente="clr-namespace:ControleAcesso.Controle.Componente"
    xmlns:utilidade="clr-namespace:ControleAcesso.Utilidade;assembly=ControleAcesso"
    x:Class="ControleAcesso.Controle.Componente.Grade"
    AlinhamentoExterno="-5, 0, -5, 0">

    <StackLayout>

        <Label
            Text="{Binding Titulo}"
            TextColor="{Binding TituloCor}"
            FontSize="{Binding TituloTamanho}"/>
        <Label
            Margin="20, 5, 0, 0"
            Text="{x:Static utilidade:Constantes.TextoNenhumaInformacao}"
            TextColor="Gray"
            FontSize="12"
            IsVisible="{Binding TextoNenhumaInformacaoVisivel}"/>
        <Grid
            x:Name="Leiaute"
            Margin="-6, 10, -3, 1"
            Padding="6, 0, 9, 0"
            IsVisible="{Binding LeiauteVisivel}">
        </Grid>
    </StackLayout>

</componente:_ComponenteBase>